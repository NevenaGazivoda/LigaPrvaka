@model IEnumerable<LigaPrvaka.ModelEF.Igraci>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayName("Tim za koji igra")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Timovi.Naziv)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PKIgracID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PKIgracID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PKIgracID })
        </td>
    </tr>
}

</table>
